# M4 Compatibility Set

本文固定M4 Conversation Orchestrator与Main AI的实现、测试和安全边界。M4只允许synthetic fixture、deterministic model/Telegram fake和disposable PostgreSQL/Redis；真实Telegram Session、Bot polling、provider credential和真实AUTO发送均为`NOT RUN`。

## 固定组件

| 组件 | 固定版本或契约 | M4用途 |
|---|---|---|
| CPython | 3.14.7 | 本地与GitLab执行环境 |
| PostgreSQL / pgvector | 17.10 / 0.8.6 | turn、run、grace、draft、control command和intent durable truth |
| Redis | 8.2.8 | 沿用M1通知兼容面；M4正确性不依赖缓存 |
| Model port | `canonical-model-port-v1` | 只向注入fake发送run ID、logical role和input fingerprint |
| Telegram port | M3 stable random ID contract | 只使用注入fake执行read、typing和text send |

Alembic head为`0005_m4_control_result`。迁移链验证empty→head、head→base→head、`0001`→head、`0002`→head、`0003`→head和`0004`→head，并执行`m1_roles.sql`至`m4_roles.sql`。M4新增11张表，总表数为51（含`alembic_version`）；`0005`为`control_commands`补齐Bot私聊、expected/result version、metadata-only status payload和terminal-result语义约束，M3暂缓的turn/model-run宽外键在`0004`关闭。

## Mode与turn

resolver的固定优先级为maintenance、global pause、contact pause、temporary HUMAN、conversation override/account default；operational BLOCKED与base mode分开保存。account control、conversation mode、content revision和turn generation均是单调版本，所有会产生模型调用或Telegram副作用的记录保存适用snapshot。

AUTO eligible incoming使用sliding 3秒debounce与10秒hard cap。turn membership逐条绑定message revision和source event；同一conversation至多一个collecting turn，以及一个ready/generating/output-ready turn。HUMAN、COPILOT incoming、PAUSED和BLOCKED不自动创建响应turn。

## Generation与三秒规则

Main AI run绑定active profile/config/credential version、input fingerprint、prompt/capability hash、attempt、usage和结果hash。provider timeout与incoming race的3秒deadline是两个独立概念。

只有run开始后的新eligible incoming可以获得grace：完整模型结果必须在`t0 + 3s`前完成，且authorization逐项保存当时允许忽略的event ID。Telegram RPC前再次比较实际delta；后续edit、delete、human outgoing、mode/control变化或新增事件不能借用旧authorization。超过deadline的run进入superseded，外部model task被best-effort cancel，晚结果只读取terminal状态而不创建第二条发送路径；pending incoming形成replacement turn。

## 发送、feedback与恢复

输出先规范化和确定性分片，再在同一事务创建delivery group与ordered intents；每段保留stable `telegram_random_id`。首个RPC前要求完整content snapshot或精确grace；首个intent claim后以`first_side_effect_at`记录下一步RPC可能产生副作用的保守边界，但只有`sent_count > 0`才启用continuation语义，首段FloodWait/transient retry不能借此绕过content revision。后续chunk不因本group outgoing observation或新incoming造成的content revision变化误取消，但仍验证原turn membership未edit/delete/redact、没有confirmed human outgoing、mode/control/lease有效，并只在较小ordinal已经durable sent后claim；unknown仍先reconcile。read acknowledgement与typing只在AUTO generation启动；4秒heartbeat续租generation并刷新typing，终止、取消、失败和正常完成均best-effort停止typing。COPILOT/HUMAN/PAUSED不自动read或typing。

confirmed human outgoing、edit/delete、pause、maintenance和control version变化会取消仍未产生Telegram副作用的turn/run/group/intent/draft。已开始或unknown的RPC继续按M3 random-ID reconciliation处理，不盲目重发。全部outgoing chunks核对后，turn和COPILOT draft才进入terminal sent/completed，coverage前进并创建幂等Memory refresh job。恢复AUTO只推进resume floor，不自动补历史；`/reply_pending`是显式且仍受完整门禁约束的例外。

## COPILOT与Control Bot

响应式COPILOT只由`/draft <opaque-target>`触发。draft、immutable revision、30分钟expiry、Send/Edit/Ignore action token和approval均持久化；token绑定allowlisted admin、Bot chat、purpose、draft revision和deadline，并且只能使用一次。Edit创建新revision；Send仍创建`copilot_approved` durable intents并重跑final gate。

Control Bot命令边界只接受allowlisted管理员私聊，支持account/scoped `/ai`、`/human`、`/copilot`、`/mode_inherit`、`/pause`、`/resume`、`/draft`、`/reply_pending`、`/cancel`、`/takeover_end`和metadata-only `/status`。contact参数只接受通过AES-GCM加密认证、并绑定deployment、account、conversation、allowlisted admin、Bot私聊和expiry的短期`ct_...` token，不以display name作为身份键。control backend只插入`control_commands`与requested outbox，首次返回queued；app executor使用`FOR UPDATE SKIP LOCKED`锁定pending command、在执行时绑定expected version并写applied/rejected、result version、`result_changed`、metadata-only status payload与completed outbox。control role不能读取conversation/content表或UPDATE command/orchestrator state。同一update重放返回既有pending/terminal结果，不重复创建turn、重新验证已过期target token或递增版本。Bot polling、contact picker UI和进程启动wiring属于后续runtime/operations集成。

## 已验证与未验证边界

此前签名提交`6185c6ef0a33ca4d8fadc293b49a536a18d7e24a`及GitLab pipeline [#2752242512](https://gitlab.com/Septuagintks/telegram_userbot/-/pipelines/2752242512)的基线证据仍可追溯，但本轮审查已重开M4并修复continuation gate、queued Control Bot backend/app executor与证据生成器。race manifest现从JUnit stable test ID派生，缺失、skip或失败均不能自报`PASS`；acceptance M4-001—M4-010同样绑定JUnit结果。本轮Windows/CPython 3.14.7有227个default测试`PASS`、31个非默认测试deselect，line coverage 89.36%、branch coverage 80.70%，Ruff、strict mypy、import boundary和compileall为`PASS`；原80% branch gate保持不变。本机无容器运行时，M4 PostgreSQL integration与disposable PostgreSQL/Redis为`NOT RUN`，本地race writer据此正确失败关闭；command/outbox→app executor权限边界已完成；GitLab Linux重验证仍待完成。

M4当前为`REOPENED — CI PENDING`；M4-011和M5启动保持等待，直到新的签名候选提交通过GitLab disposable service、migration、JUnit-derived race与acceptance证据。默认CLI仍不会构造Telethon client、读取Session、启动Control Bot/Web App、解密provider key或运行AUTO scheduler；真实Telegram/provider、Ubuntu production、backup/restore、production load和24小时soak继续为`NOT RUN`。
